name: chat_message_composer_example
description: Пример использования chat_message_composer
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.41.9"

dependencies:
  flutter:
    sdk: flutter
  chat_message_composer:
    path: ../
  path_provider: ^2.1.5
  shared_preferences: ^2.5.5
  equatable: ^2.0.8
  delta_text_view: ^1.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
